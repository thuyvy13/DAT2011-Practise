# 20. Lọc phần tử trùng lặp: Tạo một danh sách có các phần tử trùng lặp và chuyển đổi nó thành set để loại bỏ các phần tử trùng.
my_list = [1, 2, 3, 2, 3, 5]
filter_set = set(my_list)
print("List: ", my_list)
print("Danh sách loại bỏ phần tử trùng: ", filter_set)