# 26. Đếm số lượng từ trong chuỗi
# Nhập một chuỗi và sử dụng vòng lặp để đếm số lượng từ trong chuỗi đó.
chuoi = input("Nhập một chuỗi: ")
so_tu = len(chuoi.split())
print(f"Số lượng từ trong chuỗi là: {so_tu}")
